Ce dossier contient des fichiers d'exemple pour les tests.

- `mapping.csv` : exemple de cartographie (colonnes : path, level, permissions).

Remplace `mapping.csv` par ton fichier Excel `mapping.xlsx` si nécessaire, ou ajoute simplement ton `mapping.xlsx` dans ce dossier.
